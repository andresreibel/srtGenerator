import re; def srt_time_to_ms(srt_time): h, m, s = srt_time.split(":"); s, ms = s.split(","); return (int(h) * 3600 + int(m) * 60 + int(s)) * 1000 + int(ms); with open("/Users/master/code/srtGenerator/output_srt_files/mvp1_en.srt", "r") as f: content = f.read(); timestamps = re.findall(r"(\d{2}:\d{2}:\d{2},\d{3}) --> (\d{2}:\d{2}:\d{2},\d{3})", content); invalid = []; for i, (start, end) in enumerate(timestamps, 1): start_ms = srt_time_to_ms(start); end_ms = srt_time_to_ms(end); if end_ms <= start_ms: invalid.append((i, start, end, end_ms - start_ms)); print("Invalid timestamps found:" if invalid else "No invalid timestamps found"); [print(f"Subtitle #{i}: {start} --> {end} (diff: {diff}ms)") for i, start, end, diff in invalid];
